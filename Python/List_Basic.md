---

# 🐍 Python 리스트 입문 탈출: 중급 실습 문제 10선

### [문제 1] 인덱싱으로 양수와 음수 위치 매칭하기

동일한 원소에 접근할 때 양수 인덱스와 음수 인덱스를 모두 사용할 수 있습니다. 아래 리스트에서 값 `30`을 출력하려고 합니다. 빈칸에 알맞은 인덱스 숫자를 각각 채워 넣으세요.

```python
fruits = [10, 20, 30, 40, 50]

# 양수 인덱스 사용
print(fruits[____])  # 출력 예시: 30

# 음수 인덱스 사용
print(fruits[____])  # 출력 예시: 30

```

---

### [문제 2] 슬라이싱으로 특정 구간만 쏙 빼내기

주어진 전체 리스트에서 인덱스 1번부터 3번까지의 원소만 안전하게 잘라내어 새로운 리스트 `sub_list`에 저장하려고 합니다. 파이썬 슬라이싱의 `[시작:종료]` 규칙을 생각하여 빈칸을 완성하세요.

```python
colors = ['red', 'green', 'blue', 'yellow', 'purple']

# green, blue, yellow만 추출되도록 슬라이싱 범위를 지정하세요.
sub_list = colors[____]

print(sub_list)
# 출력 예시: ['green', 'blue', 'yellow']

```

---

### [문제 3] 리스트의 끝에 새로운 요소 쌓기

비어 있는 리스트 `shopping_list`가 있습니다. 리스트 메서드를 활용하여 `'apple'`, `'banana'`, `'cherry'`를 순서대로 하나씩 추가하여 최종 리스트를 완성하는 코드를 작성하세요.

```python
shopping_list = []

# 이곳에 세 개의 과일을 순서대로 추가하는 코드를 작성하세요.


print(shopping_list)
# 출력 예시: ['apple', 'banana', 'cherry']

```

---

### [문제 4] 조건에 맞는 원소의 개수 세기

정수들이 담긴 리스트에서 `50`보다 큰 숫자가 총 몇 개인지 세어보고 싶습니다. 반복문(`for`)과 조건문(`if`)을 활용하여 개수를 누적하는 코드를 완성하세요.

```python
numbers = [25, 60, 45, 80, 55, 30, 90]
count = 0

for num in numbers:
    # 이곳에 50보다 큰 경우 count를 1씩 증가시키는 코드를 작성하세요.


print(count)
# 출력 예시: 4

```

---

### [문제 5] 특정 위치에 요소 끼워 넣기

리스트의 맨 앞이나 맨 뒤가 아닌, 내가 원하는 특정 위치(인덱스)에 새로운 데이터를 삽입하고 싶습니다. 리스트 메서드를 사용하여 `['월', '수', '목']` 사이에 `'화'`를 올바른 위치에 끼워 넣으세요.

```python
days = ['월', '수', '목']

# '월'과 '수' 사이에 '화'를 삽입하는 코드를 작성하세요.


print(days)
# 출력 예시: ['월', '화', '수', '목']

```

---

### [문제 6] 원소의 값을 기반으로 삭제하기

리스트에 저장된 데이터 중 특정 값을 찾아서 지우고 싶습니다. 리스트 메서드를 활용하여 `animals` 리스트에서 `'mosquito'`를 찾아 삭제하는 코드를 작성하세요.

```python
animals = ['dog', 'cat', 'mosquito', 'rabbit']

# 이곳에 'mosquito'를 삭제하는 코드를 작성하세요.


print(animals)
# 출력 예시: ['dog', 'cat', 'rabbit']

```

---

### [문제 7] 리스트를 거꾸로 뒤집기 (슬라이싱 활용)

원본 리스트를 훼손하지 않고, 오직 **슬라이싱의 step(간격) 옵션**만을 활용하여 전체 리스트의 순서를 정반대로 뒤집은 새로운 리스트 `reversed_list`를 만드는 한 줄 코드를 작성하세요.

```python
letters = ['A', 'B', 'C', 'D']

# 슬라이싱을 이용해 역순 리스트를 만드세요.
reversed_list = letters[____]

print(reversed_list)
# 출력 예시: ['D', 'C', 'B', 'A']

```

---

### [문제 8] 리스트의 가장 큰 값 찾기 (동작 원리 구현)

파이썬 내장 함수 `max()`를 사용하지 않고, 반복문을 돌며 리스트 안에서 가장 큰 숫자를 찾아내는 논리를 연습해 보려고 합니다. 빈칸을 채워 최댓값 알고리즘을 완성하세요.

```python
scores = [70, 85, 95, 60, 90]
max_value = scores[0]  # 첫 번째 값을 임시 최댓값으로 설정

for score in scores:
    # 현재 score가 max_value보다 크다면 max_value를 업데이트합니다.
    if ________:
        max_value = ________

print(max_value)
# 출력 예시: 95

```

---

### [문제 9] 두 리스트를 하나로 이어 붙이기

서로 다른 두 개의 학급 명부 리스트가 있습니다. 이 두 리스트의 데이터를 합쳐서 하나의 큰 리스트 `all_students`를 만들려고 합니다. 리스트 간의 **더하기(`+`) 연산** 특성을 활용하여 빈칸을 채우세요.

```python
class_A = ['Kim', 'Lee']
class_B = ['Park', 'Choi']

# 두 리스트를 연결하여 하나의 리스트로 만드세요.
all_students = 

print(all_students)
# 출력 예시: ['Kim', 'Lee', 'Park', 'Choi']

```

---

### [문제 10] 리스트의 전체 요소를 두 배로 만들기

주어진 정수 리스트의 모든 원소에 각각 2를 곱하여 값을 업데이트하고 싶습니다. **인덱스를 활용한 반복문**을 사용하여 리스트 내부의 값을 직접 수정하는 코드를 완성하세요.

```python
data = [1, 2, 3, 4, 5]

# 인덱스를 사용하여 각 원소의 값을 2배로 변경하는 반복문을 작성하세요.
for i in range(len(data)):
    data[i] = ________

print(data)
# 출력 예시: [2, 4, 6, 8, 10]

```
