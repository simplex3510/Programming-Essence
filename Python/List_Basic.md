선생님이 의도하신 **'학생들이 직접 코드를 작성하며 개념과 논리를 강화할 수 있는 실습형 문제'** 10개를 마크다운 형식으로 구성했습니다.

단순히 결과만 맞추는 퀴즈가 아니라, 조건에 맞춰 빈칸을 채우거나 직접 알고리즘을 설계해야 하는 중상급 난이도의 실습 과제들입니다. 수업용이나 과제용으로 바로 복사해서 활용하실 수 있도록 **답안을 제외하고** 깔끔하게 정리했습니다.

---

# 🐍 Python 리스트 마스터: 논리 강화 실습 문제 10선

### [문제 1] 슬라이싱을 이용한 회문(Palindrome) 검사 함수

사용자가 입력한 단어가 앞으로 읽으나 뒤로 읽으나 똑같은 단어(회문)인지 확인하려고 합니다. 슬라이싱의 특성을 활용하여, 입력받은 문자열 리스트가 회문이면 `True`, 아니면 `False`를 반환하는 함수를 **for문 없이 단 한 줄의 return문으로** 완성하세요.

```python
def is_palindrome(word_list):
    # 이곳에 코드를 작성하세요
    return 

# 테스트 코드
print(is_palindrome(['t', 'o', 'm', 'a', 't', 'o']))  # 출력 예시: True
print(is_palindrome(['a', 'p', 'p', 'l', 'e']))      # 출력 예시: False

```

---

### [문제 2] 2차원 리스트 행(Row) 뒤집기 구현

3x3 크기의 2차원 리스트를 입력받아, 중간 행(인덱스 1)을 기준으로 위아래 행의 위치를 바꾸는 코드를 작성하세요. (단, `reverse()` 메서드나 새로운 리스트를 생성하지 말고 **오직 리스트 슬라이싱과 대입 연산만** 사용해야 합니다.)

```python
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

# 이곳에 코드를 작성하여 matrix의 행을 뒤집으세요.

print(matrix) 
# 출력 예시: [[7, 8, 9], [4, 5, 6], [1, 2, 3]]

```

---

### [문제 3] 인덱스 밀림 현상 방지를 이용한 값 제거

주어진 정수 리스트에서 짝수를 모두 제거하는 코드를 구현하고자 합니다. 반복문 진행 중 원소가 삭제되면서 인덱스가 앞으로 밀려 요소를 건너뛰는 함정을 피하기 위해, **리스트의 끝(뒤쪽)에서부터 원소를 검사하여 삭제하는 방식**으로 코드를 완성하세요.

```python
numbers = [2, 4, 3, 6, 5, 8, 10, 7]

# 이곳에 뒤에서부터 역순으로 인덱스를 조회하며 짝수를 제거하는 코드를 작성하세요.

print(numbers)
# 출력 예시: [3, 5, 7]

```

---

### [문제 4] 리스트 메서드를 활용한 회전(Rotation) 구현

정수 리스트와 회전 횟수 `K`가 주어집니다. 리스트의 맨 뒤에 있는 원소를 꺼내어 맨 앞으로 이동시키는 작업을 `K`번 반복하는 프로그램을 작성하세요. (단, 복잡한 인덱싱 연산 대신 `pop()`과 `insert()` 메서드의 특징을 활용해야 합니다.)

```python
data = [10, 20, 30, 40, 50]
K = 2

# 이곳에 K번 회전시키는 논리를 작성하세요.

print(data)
# 출력 예시 (2번 회전): [40, 50, 10, 20, 30]

```

---

### [문제 5] 확장 슬라이싱(Extended Slicing)을 이용한 원소 교체

5개의 원소를 가진 `values` 리스트가 있습니다. 인덱스가 홀수인 위치(1, 3번째)의 원소들을 한 번에 알파벳 `'X'`로 변경하려고 합니다. 불연속적인 슬라이싱 대입의 규칙(개수 일치)을 고려하여 빈칸을 채우세요.

```python
values = [10, 20, 30, 40, 50]

# 빈칸을 채워 인덱스 1, 3번의 값을 'X'로 바꾸세요.
values[____] = [____]

print(values)
# 출력 예시: [10, 'X', 30, 'X', 50]

```

---

### [문제 6] 중첩 리스트의 올바른 독립 초기화

`[[0, 0, 0], [0, 0, 0], [0, 0, 0]]` 형태의 3x3 바둑판 배열을 만들려고 합니다. 곱하기 연산자(`*`)를 무분별하게 사용하여 주소가 공유되는 문제를 피하고, 각 행이 메모리 상에서 완벽히 독립된 객체로 존재하도록 리스트 컴프리헨션(List Comprehension)을 사용하여 배열을 초기화하는 코드를 작성하세요.

```python
# 이곳에 각 행이 독립적인 3x3 2차원 리스트를 생성하는 한 줄 코드를 작성하세요.
board = 

# 주소 공유 여부 테스트용 코드 (수정 시 0행만 바뀌어야 함)
board[0][0] = 1
print(board)
# 출력 예시: [[1, 0, 0], [0, 0, 0], [0, 0, 0]]

```

---

### [문제 7] 리스트를 이용한 스택(Stack) 구조 모방

리스트의 맨 끝을 탑(Top)으로 삼아 데이터를 쌓고 꺼내는 스택 구조를 구현하려 합니다. 비어 있는 리스트 `stack`에 원소 `10, 20, 30`을 순서대로 넣은 뒤, 가장 마지막에 들어간 원소부터 차례대로 꺼내어 출력하는 코드를 완성하세요. (단, 리스트 전용 메서드인 `append()`와 인자가 없는 `pop()`만을 사용해야 합니다.)

```python
stack = []

# 1. stack에 10, 20, 30을 순서대로 추가하는 코드 작성


# 2. 반복문을 사용하여 stack이 빌 때까지 요소를 하나씩 꺼내어 출력하는 코드 작성
while ________:
    print(________)

# 출력 예시:
# 30
# 20
# 10

```

---

### [문제 8] 슬라이싱 범위를 초과하는 인덱스의 안전 메커니즘 활용

파이썬의 슬라이싱은 인덱스 범위를 초과해도 예외를 내지 않는 안전장치가 있습니다. 이 특성을 이용하여 길이가 유동적인 리스트에서 "앞에서부터 최대 3개의 원소"와 "뒤에서부터 최대 3개의 원소"를 각각 추출해 새로운 리스트로 합치는 코드를 작성하세요.

```python
def extract_edges(lst):
    # 이곳에 코드를 작성하세요.
    # 리스트의 길이가 3보다 작더라도 에러가 나지 않아야 합니다.
    return 

# 테스트 코드
print(extract_edges([1, 2, 3, 4, 5, 6, 7]))  # 출력 예시: [1, 2, 3, 5, 6, 7]
print(extract_edges([1, 2]))                # 출력 예시: [1, 2, 1, 2]

```

---

### [문제 9] 얕은 복사(Shallow Copy) 환경에서의 2차원 원소 제어

`original` 리스트를 슬라이싱(`[:]`) 기법으로 얕은 복사하여 `copied` 리스트를 만들었습니다. 복사본 리스트를 조작할 때 원본 리스트의 값도 함께 변하는 성질을 이용하여, **`copied` 변수만을 조작하여 원본 `original` 리스트의 상태를 바꾸는** 코드를 완성하세요.

```python
original = [[10, 20], [30, 40]]
copied = original[:]

# 이곳에 copied를 조작하는 코드를 작성하세요.
# 단, original 변수를 직접 언급하거나 사용해서는 안 됩니다.


print(original)
# 출력 예시: [[10, 999], [30, 40]]

```

---

### [문제 10] 두 리스트의 인터락킹(Interlocking) 병합

길이가 같은 두 개의 리스트 `list_A`와 `list_B`가 주어집니다. 두 리스트의 원소를 번갈아가며 엮어서 하나의 새로운 리스트 `merged`를 만들려고 합니다. 인덱스 조작 능력을 활용하여 이 논리를 수행하는 반복문 코드를 완성하세요.

```python
list_A = ['A', 'B', 'C']
list_B = [1, 2, 3]
merged = []

# 반복문을 활용하여 merged 리스트에 ['A', 1, 'B', 2, 'C', 3] 순서로 값이 들어가도록 구현하세요.


print(merged)
# 출력 예시: ['A', 1, 'B', 2, 'C', 3]

```
